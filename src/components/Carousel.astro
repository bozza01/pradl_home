---
const { images } = Astro.props;
---

<div class="carousel">
  {images.map((src) => (
    <img src={src} loading="lazy" />
  ))}
</div>

<style>
.carousel {
  display: flex;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  gap: 1rem;
  padding-bottom: 1rem;
}

.carousel img {
  width: 100%;
  max-width: 600px;
  border-radius: 12px;
  scroll-snap-align: start;
  flex-shrink: 0;
}
</style>